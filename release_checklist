1. update CHANGELOG
2. bump version number in `setup.py` and in `__init__.py`
3. wait for GitHub CI workflow to complete
4. download dist artifact
5. delete linux wheels: `rm -f dist/*linux*.whl`
6. upload to PyPI: `python3 -m twine upload dist/*.whl dist/*.gz`
